# 공고 매칭 1-A (테이블 · 추출 API · 매칭 엔진) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 공고 자격조건을 담을 테이블·추출 API와, `companies` 404개를 유력/애매/제외로 가르는 **순수 함수 매칭 엔진**을 만든다. 화면은 1-B에서 붙인다.

**Architecture:** 매칭은 **클라이언트에서 도는 순수 함수**다. `companies` 는 이미 App.js 메모리에 있으므로 supabase 조회가 한 건도 안 늘고, 재매칭이 즉시·무료다. 조건 추출만 AI(`/api/parse-announcement`)를 쓴다. 엔진 함수는 App.js 안에 **표식(sentinel) 주석으로 감싼 한 덩어리**로 두고, 테스트 스크립트가 그 블록을 소스에서 떼어내 실제 DB 덤프로 실행한다(이 저장소의 `test-nocard.mjs`·`test-debtor-change.mjs` 와 같은 방식 — 손으로 옮겨 적으면 코드 검증이 아니다).

**Tech Stack:** React(CRA, 인라인 스타일) · Supabase(PostgREST + RLS) · Vercel 서버리스 함수 · Anthropic Messages API(`claude-opus-5`) · 검증은 `node scripts/*.mjs` (테스트 프레임워크 없음)

**Spec:** `docs/superpowers/specs/2026-08-30-공고매칭-design.md`

## Global Constraints

- **공용 함수를 고치지 않는다.** `bizScaleCap` · `bizAvgRevenue` · `judgeSmallBiz` · `judgeSososang` · `bizOverride` · `BIZ_SCALE_TABLE` 은 **호출만** 한다. 기업목록 규모 배지가 같이 본다.
- **추측 금지.** 조건이 `null` 이면 "공고에 안 적혀 있음" = 그 조건은 안 본다. `0` 이나 `"전국"` 으로 채우지 않는다.
- **판정 3값은 `"pass" | "unknown" | "fail"` 문자열이다.** 다른 값을 쓰지 않는다.
- **판정 라벨 3종은 `"유력" | "애매" | "제외"`.** `"확실"` 은 2차(드라이브 검증) 전까지 만들지 않는다.
- **종합 규칙**: `fail` 하나라도 → `제외` / `fail` 없고 `unknown` 없음 → `유력` / 나머지 → `애매`.
- **새 테이블은 만든 커밋에서 RLS·GRANT 회수까지** (CLAUDE.md 2-2 체크리스트 전부).
- **`/api/*` 새 함수는 첫 줄에 `denyUnauthorized(req, res)`**, 파일 하나로 완결(`_` 시작 공유 모듈 금지 — Vercel이 번들에 안 넣는다).
- **`run-sql.js` 는 마지막 SELECT 하나만 출력한다.** 검증은 **반드시 별도 파일로 다시** 돌린다(2-2).
- **SQL 파일 3종 세트**: `<이름>.sql` · `<이름>_rollback.sql` · `<이름>_검증.sql` (저장소 관례).
- **모바일 영향 0** — 이 차수에는 렌더 코드가 없다.
- 커밋 메시지 끝에:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP`

### 이름 충돌 확인 (2026-08-30 실측)
`normRegion` · `ann*` · `ANN_` 은 App.js 에 **0건**. `announcement` 20건은 전부 `is_announcement`(team_notes 팀 공지 컬럼)이며 **이 기능과 무관하다 — 헷갈리지 말 것.**

---

## File Structure

| 파일 | 책임 |
|---|---|
| `src/App.js` (수정) | 매칭 엔진 함수 전체. `// ── ANN-ENGINE-START ──` ~ `// ── ANN-ENGINE-END ──` 표식 사이 한 덩어리 |
| `src/App.js` (수정) | 기존 규모 판정 블록에 `// ── BIZ-SCALE-START/END ──` 표식만 추가 (**주석뿐, 로직 0줄 변경**) |
| `scripts/test-ann-match.mjs` (신규) | 표식 사이를 떼어내 실행하는 검증 스크립트. Task 1~4가 케이스를 쌓아 간다 |
| `공고매칭_announcements_테이블추가.sql` (신규) | 테이블 + RLS + GRANT 회수 |
| `공고매칭_announcements_테이블추가_rollback.sql` (신규) | 되돌리기 |
| `공고매칭_announcements_테이블추가_검증.sql` (신규) | 별도 실행 검증 |
| `api/parse-announcement.js` (신규) | 공고 → 조건 JSON 추출. `parse-credit.js` 의 형제 |

**Interfaces (전 태스크 공통 — 나중 태스크가 이 이름에 의존한다)**

```
normRegion(raw)                        → { sido: string, sigungu: string }
annTrimGu(raw)                         → string
annDeeperThanCity(sigunguRaw)          → boolean
annRegionState(coRegion, includeList)  → "pass"|"unknown"|"fail"
annIndustryTokens(raw)                 → string[]
annIndustryState(coIndustry, industries) → "pass"|"unknown"|"fail"
annScaleState(co, scale)               → "pass"|"unknown"|"fail"
annRevenueState(co, rev)               → "pass"|"unknown"|"fail"
annAgeState(co, age, todayYm)          → "pass"|"unknown"|"fail"
annEmployeeState(co, emp)              → "pass"|"unknown"|"fail"
annEvalCompany(co, cond, todayYm)      → { verdict, checks, notes }
annRunMatch(companies, cond, ranBy)    → { summary, rows }
ANN_ENGINE_VERSION                     → 1
```

---

## Task 1: 지역 정규화 · 지역 조건 판정

세 출처(CRM `서울_강남` · 공고 `서울특별시 강남구` · 서류 도로명주소)가 **같은 함수를 통과**해야 비교가 성립한다.

**Files:**
- Modify: `src/App.js` — `judgeSososang` 함수의 닫는 `}` 바로 다음 줄에 표식 + 새 블록 삽입
- Create: `scripts/test-ann-match.mjs`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces: `ANN_SIDO_ALIAS` · `normRegion(raw)` · `annTrimGu(raw)` · `annDeeperThanCity(raw)` · `annRegionState(coRegion, includeList)` · 표식 `// ── ANN-ENGINE-START ──` / `// ── ANN-ENGINE-END ──` / `// ── BIZ-SCALE-START ──` / `// ── BIZ-SCALE-END ──`

- [ ] **Step 1: App.js 에 표식 주석 4개를 넣는다 (로직 0줄 변경)**

`src/App.js` 에서 `// ═══...` 로 시작하는 「📊 소기업/소상공인 규모 자동판정」 배너 주석 **바로 위**에 추가:

```js
// ── BIZ-SCALE-START ── (scripts/test-ann-match.mjs 가 이 표식 사이를 떼어내 실행한다. 지우지 말 것)
```

`judgeSososang` 함수의 닫는 `}` **바로 다음 줄**에 추가:

```js
// ── BIZ-SCALE-END ──
```

그리고 그 `// ── BIZ-SCALE-END ──` **바로 다음 줄**에 새 블록의 시작·끝 표식을 넣는다(내용은 Step 3에서 채운다):

```js

// ══════════════════════════════════════════════════════════════════════════
// 📢 공고 매칭 엔진 — 순수 함수만. supabase·React 를 부르지 않는다.
//    scripts/test-ann-match.mjs 가 아래 표식 사이를 통째로 떼어내 실제 DB 덤프로 실행한다.
//    ⚠️ 이 블록 안에서 App.js 의 다른 것을 참조하려면 BIZ-SCALE 블록 안에 있어야 한다.
// ══════════════════════════════════════════════════════════════════════════
// ── ANN-ENGINE-START ──
// ── ANN-ENGINE-END ──
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

Create `scripts/test-ann-match.mjs`:

```js
// 공고 매칭 엔진 검증 (2026-08-30)
// ⚠️ App.js 소스에서 표식 사이를 **떼어내 그대로 실행**한다 — 손으로 옮겨 적으면 코드 검증이 아니다.
//    (test-nocard.mjs · test-debtor-change.mjs 와 같은 방식)
// ⚠️ 읽기 전용이다. 아무것도 쓰지 않는다.
// 사용법: node scripts/test-ann-match.mjs
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const src = fs.readFileSync(path.join(ROOT, "src/App.js"), "utf8");

function slice(startMark, endMark) {
  const a = src.indexOf(startMark);
  const b = src.indexOf(endMark);
  if (a < 0) { console.error("❌ App.js 에서 " + startMark + " 를 못 찾았습니다"); process.exit(1); }
  if (b < a) { console.error("❌ App.js 에서 " + endMark + " 를 못 찾았습니다"); process.exit(1); }
  return src.slice(a + startMark.length, b);
}

const bizSrc = slice("// ── BIZ-SCALE-START ──", "// ── BIZ-SCALE-END ──");
const annSrc = slice("// ── ANN-ENGINE-START ──", "// ── ANN-ENGINE-END ──");
console.log(`── 떼어낸 소스: 규모판정 ${bizSrc.trim().split("\n").length}줄 · 매칭엔진 ${annSrc.trim().split("\n").length}줄 ──`);

const EXPORTS = [
  "normRegion", "annTrimGu", "annDeeperThanCity", "annRegionState",
];
const mod = new Function(bizSrc + "\n" + annSrc + "\nreturn {" + EXPORTS.join(",") + "};")();

let pass = 0, fail = 0;
function eq(label, got, want) {
  const g = JSON.stringify(got), w = JSON.stringify(want);
  if (g === w) { pass += 1; }
  else { fail += 1; console.error(`❌ ${label}\n   got  ${g}\n   want ${w}`); }
}

// ── normRegion ──────────────────────────────────────────────────────────────
const R = mod.normRegion;
eq("CRM 언더바",        R("서울_강남"),                       { sido: "서울", sigungu: "강남" });
eq("CRM 공백",          R("경기 시흥"),                       { sido: "경기", sigungu: "시흥" });
eq("공고 정식표기",     R("서울특별시 강남구"),               { sido: "서울", sigungu: "강남" });
eq("공고 도명",         R("경기도 시흥시"),                   { sido: "경기", sigungu: "시흥" });
eq("시도만",            R("경기"),                            { sido: "경기", sigungu: "" });
eq("도로명주소",        R("서울특별시 강남구 테헤란로 123"),  { sido: "서울", sigungu: "강남" });
eq("붙여쓴 표기",       R("서울특별시강남구"),                { sido: "서울", sigungu: "강남" });
eq("중구(한 글자 남음)", R("서울_중구"),                      { sido: "서울", sigungu: "중" });
eq("강원특별자치도",    R("강원특별자치도 원주시"),           { sido: "강원", sigungu: "원주" });
eq("전북특별자치도",    R("전북특별자치도 전주시"),           { sido: "전북", sigungu: "전주" });
eq("충남",              R("충남_논산"),                       { sido: "충남", sigungu: "논산" });
eq("빈 값",             R(""),                                { sido: "", sigungu: "" });
eq("못 읽는 값",        R("미정"),                            { sido: "", sigungu: "" });
// ⚠️ 광주 — 시도와 쌍으로 봐야만 갈린다
eq("광주광역시",        R("광주광역시 광산구"),               { sido: "광주", sigungu: "광산" });
eq("경기도 광주시",     R("경기_광주"),                       { sido: "경기", sigungu: "광주" });

// ── annTrimGu ───────────────────────────────────────────────────────────────
eq("접미사 제거 구",  mod.annTrimGu("강남구"), "강남");
eq("접미사 제거 시",  mod.annTrimGu("시흥시"), "시흥");
eq("한 글자는 유지",  mod.annTrimGu("구"),     "구");
eq("접미사 없음",     mod.annTrimGu("강남"),   "강남");

// ── annDeeperThanCity ───────────────────────────────────────────────────────
eq("시 아래 구",   mod.annDeeperThanCity("고양시 덕양구"), true);
eq("시 단위",      mod.annDeeperThanCity("시흥시"),        false);
eq("구 단위",      mod.annDeeperThanCity("강남구"),        false);
eq("군 아래 면",   mod.annDeeperThanCity("양평군 서종면"), true);

// ── annRegionState ──────────────────────────────────────────────────────────
const S = mod.annRegionState;
eq("전국(빈 배열)",   S("서울_강남", []),                                     "pass");
eq("전국(null)",      S("서울_강남", null),                                   "pass");
eq("시군구 일치",     S("서울_강남", [{ sido: "서울특별시", sigungu: "강남구" }]), "pass");
eq("시도만 요구",     S("서울_강남", [{ sido: "서울특별시", sigungu: "" }]),      "pass");
eq("시도부터 다름",   S("서울_강남", [{ sido: "경기도", sigungu: "시흥시" }]),    "fail");
eq("시도같고 시군구 다름", S("서울_강남", [{ sido: "서울특별시", sigungu: "중구" }]), "fail");
eq("우리는 시도만",   S("경기",      [{ sido: "경기도", sigungu: "시흥시" }]),    "unknown");
eq("우리 지역 못 읽음", S("미정",    [{ sido: "서울특별시", sigungu: "강남구" }]), "unknown");
eq("여러 곳 중 하나 적중", S("경기_시흥", [{ sido: "서울특별시", sigungu: "" }, { sido: "경기도", sigungu: "시흥시" }]), "pass");
// ⚠️ 공고가 우리보다 깊은 단위를 요구하면 확정하지 않는다 — 일산 업체가 섞인다
eq("시 아래 구 한정",  S("경기_고양", [{ sido: "경기도", sigungu: "고양시 덕양구" }]), "unknown");
// ⚠️ 광주 오탐 — 시도가 다르면 시군구가 같아도 불일치
eq("광주 오탐 방지",   S("경기_광주", [{ sido: "광주광역시", sigungu: "" }]),      "fail");

console.log(`\n${fail === 0 ? "✅" : "❌"} ${pass}/${pass + fail} 통과`);
process.exit(fail === 0 ? 0 : 1);
```

- [ ] **Step 3: 테스트를 돌려 실패를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **FAIL** — `❌ App.js 에서 // ── ANN-ENGINE-START ── 를 못 찾았습니다` 는 나오지 않고(Step 1에서 표식을 넣었으므로),
`normRegion is not defined` 계열 오류로 죽는다.

- [ ] **Step 4: 최소 구현을 쓴다**

`src/App.js` 의 `// ── ANN-ENGINE-START ──` 와 `// ── ANN-ENGINE-END ──` **사이**에 넣는다:

```js
// 시도 별칭 → 표준 시도명. 길이 내림차순으로 정렬해 두므로(아래 sort) "서울"이 "서울특별시"를 먼저 먹지 않는다.
// ⚠️ bare "광주"·"경기"도 넣는다. 시도 자리에 온 값만 여기로 들어오고,
//    `경기 광주`(경기도 광주시) ↔ `광주광역시` 충돌은 annRegionState 가 시도와 쌍으로 비교해 막는다.
const ANN_SIDO_ALIAS = [
  ["서울특별시", "서울"], ["서울시", "서울"], ["서울", "서울"],
  ["부산광역시", "부산"], ["부산시", "부산"], ["부산", "부산"],
  ["대구광역시", "대구"], ["대구시", "대구"], ["대구", "대구"],
  ["인천광역시", "인천"], ["인천시", "인천"], ["인천", "인천"],
  ["광주광역시", "광주"], ["광주시", "광주"], ["광주", "광주"],
  ["대전광역시", "대전"], ["대전시", "대전"], ["대전", "대전"],
  ["울산광역시", "울산"], ["울산시", "울산"], ["울산", "울산"],
  ["세종특별자치시", "세종"], ["세종시", "세종"], ["세종", "세종"],
  ["경기도", "경기"], ["경기", "경기"],
  ["강원특별자치도", "강원"], ["강원도", "강원"], ["강원", "강원"],
  ["충청북도", "충북"], ["충북", "충북"],
  ["충청남도", "충남"], ["충남", "충남"],
  ["전북특별자치도", "전북"], ["전라북도", "전북"], ["전북", "전북"],
  ["전라남도", "전남"], ["전남", "전남"],
  ["경상북도", "경북"], ["경북", "경북"],
  ["경상남도", "경남"], ["경남", "경남"],
  ["제주특별자치도", "제주"], ["제주도", "제주"], ["제주", "제주"],
].sort(function (a, b) { return b[0].length - a[0].length; });

// 시군구 접미사 제거 — 떼고도 글자가 남을 때만 뗀다("중구"→"중", "구"→"구").
function annTrimGu(raw) {
  var t = String(raw || "").trim();
  if (!t) return "";
  var m = t.match(/^(.+?)(시|군|구)$/);
  return m && m[1] ? m[1] : t;
}

// 공고가 시(市) 아래 구·읍·면·동까지 한정하는가. 그러면 우리 데이터(시 단위)로는 확정할 수 없다.
function annDeeperThanCity(sigunguRaw) {
  return /(시|군)\s*\S+(구|읍|면|동)$/.test(String(sigunguRaw || "").trim());
}

// 지역 문자열 → { sido, sigungu }.
// ⚠️ 세 출처(CRM `서울_강남` · 공고 `서울특별시 강남구` · 서류 도로명주소)가 **같은 함수를
//    통과**해야 비교가 성립한다. 한쪽만 정규화하면 어긋난다.
// ⚠️ 시도를 못 읽으면 빈 값을 돌려준다 — 추측하지 않는다(호출부가 unknown 으로 받는다).
function normRegion(raw) {
  var s = String(raw || "").replace(/\s+/g, " ").trim();
  if (!s) return { sido: "", sigungu: "" };
  var parts = s.split(/[_/,·|\s]+/).filter(Boolean);
  var sido = "", i;

  for (i = 0; i < ANN_SIDO_ALIAS.length; i++) {
    if (parts[0] === ANN_SIDO_ALIAS[i][0]) { sido = ANN_SIDO_ALIAS[i][1]; break; }
  }
  if (!sido) {
    // 붙여 쓴 경우("서울특별시강남구") — 앞에서부터 가장 긴 별칭을 찾는다
    for (i = 0; i < ANN_SIDO_ALIAS.length; i++) {
      if (s.indexOf(ANN_SIDO_ALIAS[i][0]) === 0) {
        sido = ANN_SIDO_ALIAS[i][1];
        parts = [ANN_SIDO_ALIAS[i][0]].concat(
          s.slice(ANN_SIDO_ALIAS[i][0].length).split(/[_/,·|\s]+/).filter(Boolean));
        break;
      }
    }
  }
  if (!sido) return { sido: "", sigungu: "" };
  return { sido: sido, sigungu: annTrimGu(parts[1] || "") };
}

// 지역 조건 판정. includeList 가 비면 전국이라 전원 pass.
// ⚠️ 반드시 시도와 쌍으로 비교한다 — `경기 광주`와 `광주광역시`는 시군구만 보면 같아진다.
function annRegionState(coRegion, includeList) {
  if (!includeList || !includeList.length) return "pass";
  var co = normRegion(coRegion);
  if (!co.sido) return "unknown";
  var sidoHit = false, deeper = false;
  for (var i = 0; i < includeList.length; i++) {
    var rawSg = String((includeList[i] && includeList[i].sigungu) || "");
    var w = normRegion(String((includeList[i] && includeList[i].sido) || "") + " " + rawSg);
    if (!w.sido || w.sido !== co.sido) continue;
    sidoHit = true;
    if (!w.sigungu) return "pass";                  // 공고가 시도까지만 요구
    if (!co.sigungu) { deeper = true; continue; }   // 우리는 시도만 있다
    if (w.sigungu === co.sigungu) {
      if (annDeeperThanCity(rawSg)) { deeper = true; continue; }  // 시 아래 구까지 한정 → 확정 불가
      return "pass";
    }
  }
  if (deeper) return "unknown";
  if (sidoHit) return co.sigungu ? "fail" : "unknown";
  return "fail";
}
```

- [ ] **Step 5: 테스트를 돌려 통과를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **PASS** — `✅ 34/34 통과`

- [ ] **Step 6: 빌드가 깨지지 않는지 확인한다**

Run: `CI=true npx react-scripts build`
Expected: 빌드 성공(에러 0)

- [ ] **Step 7: 커밋**

```bash
git add src/App.js scripts/test-ann-match.mjs
git commit -F - <<'EOF'
feat(공고매칭): 🗺 지역 정규화 normRegion + 지역 조건 판정

CRM `서울_강남` · 공고 `서울특별시 강남구` · 서류 도로명주소 세 표기를
같은 함수로 통과시켜 비교가 성립하게 한다.

- 시도와 쌍으로 비교해 `경기 광주` ↔ `광주광역시` 오탐을 막는다
- 공고가 우리보다 깊은 단위(고양시 덕양구)를 요구하면 unknown — 일산 업체가 섞인다
- App.js 규모판정 블록에 BIZ-SCALE 표식 주석만 추가(로직 0줄 변경)

node scripts/test-ann-match.mjs 34/34

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP
EOF
```

---

## Task 2: 업종 조건 판정

**Files:**
- Modify: `src/App.js` — `annRegionState` 아래(같은 ANN-ENGINE 블록 안)
- Modify: `scripts/test-ann-match.mjs`

**Interfaces:**
- Consumes: 없음 (독립)
- Produces: `annIndustryTokens(raw)` → `string[]` · `annIndustryState(coIndustry, industries)` → `"pass"|"unknown"|"fail"`
  - `industries` 모양: `{ include: string[], exclude: string[], ksic: string[] }`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`scripts/test-ann-match.mjs` 의 `EXPORTS` 배열에 두 이름을 더한다:

```js
const EXPORTS = [
  "normRegion", "annTrimGu", "annDeeperThanCity", "annRegionState",
  "annIndustryTokens", "annIndustryState",
];
```

그리고 `console.log(`\n${fail === 0 ? ...` 줄 **바로 위**에 붙인다:

```js
// ── annIndustryTokens ───────────────────────────────────────────────────────
eq("단일 업종",   mod.annIndustryTokens("제조업"),                    ["제조업"]);
eq("콤마 다중",   mod.annIndustryTokens("도소매업, 제조업, 건설업"),  ["도소매업", "제조업", "건설업"]);
eq("슬래시 다중", mod.annIndustryTokens("도소매/통신기기 소매업"),    ["도소매", "통신기기 소매업"]);
eq("빈 값",       mod.annIndustryTokens(""),                          []);

// ── annIndustryState ────────────────────────────────────────────────────────
const I = mod.annIndustryState;
eq("업종 조건 없음", I("음식점업", { include: [], exclude: [] }),                  "pass");
eq("조건 자체 null", I("음식점업", null),                                          "pass");
eq("include 적중",   I("제조업", { include: ["제조"], exclude: [] }),              "pass");
eq("다중값 중 적중", I("도소매업, 제조업", { include: ["제조"], exclude: [] }),    "pass");
eq("exclude 적중",   I("유흥주점업", { include: ["서비스"], exclude: ["유흥"] }),  "fail");
// ⚠️ exclude 가 include 보다 세다 — 둘 다 걸리면 fail
eq("둘 다 걸림",     I("제조업, 유흥주점업", { include: ["제조"], exclude: ["유흥"] }), "fail");
// ⚠️ "걸린 게 없다"는 "아니다"가 아니라 "모른다" — fail 이 아니다(설계 §7-2)
eq("아무것도 안 걸림", I("음식점업", { include: ["제조"], exclude: [] }),          "unknown");
eq("우리 업종 비어있음", I("", { include: ["제조"], exclude: [] }),                "unknown");
// ⚠️ exclude 만 있는 공고 — "제외 대상이 아님"을 확정하지 않는다
eq("exclude 만 있고 안 걸림", I("도소매업", { include: [], exclude: ["유흥"] }),   "unknown");
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **FAIL** — `annIndustryTokens is not defined`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/App.js` 의 `annRegionState` 함수 닫는 `}` 바로 아래(ANN-ENGINE 블록 안)에 넣는다:

```js
// 우리 industry 는 자유텍스트에 다중값이 흔하다("도소매업, 제조업, 건설업" — 실측 143건).
// 콤마·슬래시·가운뎃점으로 쪼개 토큰 배열로 만든다.
function annIndustryTokens(raw) {
  return String(raw || "")
    .split(/[,/·|]+/)
    .map(function (t) { return t.trim(); })
    .filter(Boolean);
}

// 업종 조건 판정.
// ⚠️ 이 기능에서 제일 약한 고리다 — 우리에겐 KSIC 업종코드가 없고 자유텍스트 99종뿐이다.
// ⚠️ **"걸린 게 없다"를 fail 로 만들지 말 것.** 그건 "아니다"가 아니라 "모른다"이다(설계 §7-2).
//    금속가공업이 제조업인 줄 우리 코드는 모른다 — fail 로 두면 멀쩡한 업체가 조용히 사라진다.
//    대신 추출 프롬프트가 include 키워드를 넉넉히(동의어·하위업종) 뽑게 해서 unknown 을 줄인다.
// ⚠️ exclude 가 include 보다 세다. 둘 다 걸리면 fail 이다.
function annIndustryState(coIndustry, industries) {
  var inc = (industries && industries.include) || [];
  var exc = (industries && industries.exclude) || [];
  if (!inc.length && !exc.length) return "pass";        // 업종 조건이 없는 공고
  var toks = annIndustryTokens(coIndustry);
  if (!toks.length) return "unknown";                   // 우리 업종이 비어 있다
  var hay = toks.join(" ");
  var i;
  for (i = 0; i < exc.length; i++) {
    if (exc[i] && hay.indexOf(String(exc[i])) >= 0) return "fail";
  }
  for (i = 0; i < inc.length; i++) {
    if (inc[i] && hay.indexOf(String(inc[i])) >= 0) return "pass";
  }
  return "unknown";
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **PASS** — `✅ 47/47 통과`

- [ ] **Step 5: 커밋**

```bash
git add src/App.js scripts/test-ann-match.mjs
git commit -F - <<'EOF'
feat(공고매칭): 🏭 업종 조건 판정 (다중값 · exclude 우선)

우리 industry 는 자유텍스트 99종 + 다중값 143건이라 KSIC 대조가 안 된다.
그래서 "걸린 게 없다"를 fail 이 아니라 unknown 으로 둔다 —
금속가공업이 제조업인 줄 코드는 모르고, fail 로 두면 멀쩡한 업체가 조용히 사라진다.

node scripts/test-ann-match.mjs 47/47

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP
EOF
```

---

## Task 3: 규모 · 매출 · 업력 · 근로자수 조건 판정

**Files:**
- Modify: `src/App.js` — `annIndustryState` 아래(ANN-ENGINE 블록 안)
- Modify: `scripts/test-ann-match.mjs`

**Interfaces:**
- Consumes: 기존 `judgeSmallBiz(company)` → `{status:"yes"|"no"|"unknown", …}` · `judgeSososang(company)` → 동일 · `bizAvgRevenue(company)` → `number|null` (전부 **BIZ-SCALE 블록 안. 고치지 않는다**)
- Produces: `annScaleState(co, scale)` · `annYesNo(s)` · `annRevenueState(co, rev)` · `annAgeState(co, age, todayYm)` · `annAgeFits(months, age)` · `annMonthsBetween(y, m, todayYm)` · `annEmployeeState(co, emp)`
  - `todayYm` = `연*100 + 월` (예: 2026년 8월 → `202608`)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`EXPORTS` 에 더한다:

```js
const EXPORTS = [
  "normRegion", "annTrimGu", "annDeeperThanCity", "annRegionState",
  "annIndustryTokens", "annIndustryState",
  "annScaleState", "annRevenueState", "annAgeState", "annEmployeeState",
];
```

마지막 `console.log` 줄 **바로 위**에 붙인다:

```js
// ── annRevenueState ─────────────────────────────────────────────────────────
const V = mod.annRevenueState;
const co30 = { revenue_2023: 2800000000, revenue_2024: 3000000000, revenue_2025: 3200000000, industry: "제조업" };
eq("매출 조건 없음",   V(co30, null),                                                    "pass");
eq("상한 이내",        V(co30, { min: null, max: 12000000000, basis: "연매출" }),        "pass");
eq("상한 초과",        V(co30, { min: null, max: 1000000000, basis: "연매출" }),         "fail");
eq("하한 미달",        V(co30, { min: 10000000000, max: null, basis: "연매출" }),        "fail");
eq("3년평균 기준",     V(co30, { min: null, max: 3100000000, basis: "3년평균" }),        "pass");
eq("매출 없음",        V({ industry: "제조업" }, { min: null, max: 1000000000, basis: "연매출" }), "unknown");

// ── annAgeState (오늘 = 2026-08 고정) ───────────────────────────────────────
const A = mod.annAgeState, YM = 202608;
eq("업력 조건 없음",   A({ founded_year: 2020, founded_month: 3 }, null, YM),                       "pass");
eq("1년 이상 충족",    A({ founded_year: 2020, founded_month: 3 }, { min_months: 12 }, YM),         "pass");
eq("1년 미만 미달",    A({ founded_year: 2026, founded_month: 5 }, { min_months: 12 }, YM),         "fail");
eq("설립연도 없음",    A({}, { min_months: 12 }, YM),                                               "unknown");
// ⚠️ 설립월이 없으면 최대 11개월 오차 → 기준선을 걸치면 unknown, 안 걸치면 확정한다
eq("월없음·확실히 충족", A({ founded_year: 2020 }, { min_months: 12 }, YM),                          "pass");
eq("월없음·확실히 미달", A({ founded_year: 2026 }, { min_months: 12 }, YM),                          "fail");
eq("월없음·경계 걸침",   A({ founded_year: 2025 }, { min_months: 12 }, YM),                          "unknown");
eq("업력 상한 초과",     A({ founded_year: 2010, founded_month: 1 }, { max_months: 84 }, YM),        "fail");

// ── annEmployeeState ────────────────────────────────────────────────────────
const E = mod.annEmployeeState;
eq("근로자 조건 없음", E({ employee_count: 3 }, null),                  "pass");
eq("상한 이내",        E({ employee_count: 3 }, { max: 5 }),            "pass");
eq("상한 초과",        E({ employee_count: 9 }, { max: 5 }),            "fail");
eq("하한 미달",        E({ employee_count: 3 }, { min: 5 }),            "fail");
eq("미입력",           E({}, { max: 5 }),                               "unknown");

// ── annScaleState (기존 judgeSmallBiz·judgeSososang 을 그대로 쓴다) ─────────
const C = mod.annScaleState;
const 소상 = { industry: "음식점업", revenue_2024: 300000000, employee_count: 3 };
const 큰곳 = { industry: "음식점업", revenue_2024: 5000000000, employee_count: 40 };
eq("규모 조건 없음",   C(소상, null),        "pass");
eq("소상공인 해당",    C(소상, "소상공인"),  "pass");
eq("소상공인 아님",    C(큰곳, "소상공인"),  "fail");
eq("소기업 해당",      C(소상, "소기업"),    "pass");
eq("근로자수 없으면 불가", C({ industry: "음식점업", revenue_2024: 300000000 }, "소상공인"), "unknown");
eq("업종 없으면 불가", C({ revenue_2024: 300000000, employee_count: 3 }, "소기업"),          "unknown");
eq("중소기업은 통과",  C(소상, "중소기업"),  "pass");
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **FAIL** — `annScaleState is not defined`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/App.js` 의 `annIndustryState` 닫는 `}` 아래(ANN-ENGINE 블록 안)에 넣는다:

```js
// 규모 — 기존 판정 함수를 **그대로 호출**한다. 새 규모 판정을 만들지 말 것
// (기업목록 규모 배지가 같은 함수를 본다. 두 벌이 되면 반드시 어긋난다).
function annYesNo(s) { return s === "yes" ? "pass" : s === "no" ? "fail" : "unknown"; }
function annScaleState(co, scale) {
  if (!scale) return "pass";
  if (scale === "소상공인") return annYesNo(judgeSososang(co).status);
  if (scale === "소기업")   return annYesNo(judgeSmallBiz(co).status);
  // ⚠️ 중소기업 — 우리 고객은 전원 중소기업이라는 전제로 통과시킨다.
  //    확인한 게 아니라 전제다. annEvalCompany 가 notes 에 그 사실을 적는다.
  if (scale === "중소기업") return "pass";
  return "unknown";
}

// 매출 — companies.revenue_* 는 bigint 라 값이 잘리지 않는다(2026-08-30 확인).
function annRevenueState(co, rev) {
  if (!rev || (rev.min == null && rev.max == null)) return "pass";
  var v = rev.basis === "3년평균"
    ? bizAvgRevenue(co)
    : (Number(co && co.revenue_2025) || Number(co && co.revenue_2024) || Number(co && co.revenue_2023) || null);
  if (v == null || !(v > 0)) return "unknown";
  if (rev.min != null && v < rev.min) return "fail";
  if (rev.max != null && v > rev.max) return "fail";
  return "pass";
}

// 업력 — todayYm = 연*100+월.
// ⚠️ 설립월이 없으면 업력을 한 값으로 못 정한다(최대 11개월 오차).
//    가능한 [최소, 최대] 범위로 재서, 범위가 기준선을 걸치면 unknown 으로 둔다.
//    (실측: founded_year 는 있는데 founded_month 가 없는 기업이 1건뿐이라 영향은 작다)
function annMonthsBetween(y, m, todayYm) {
  return (Math.floor(todayYm / 100) - y) * 12 + ((todayYm % 100) - m);
}
function annAgeFits(months, age) {
  if (age.min_months != null && months < age.min_months) return false;
  if (age.max_months != null && months > age.max_months) return false;
  return true;
}
function annAgeState(co, age, todayYm) {
  if (!age || (age.min_months == null && age.max_months == null)) return "pass";
  var y = parseInt(co && co.founded_year, 10);
  if (!y || y < 1900) return "unknown";
  var m = parseInt(co && co.founded_month, 10);
  var lo, hi;                                   // 가능한 업력 개월 수의 [최소, 최대]
  if (m >= 1 && m <= 12) { lo = hi = annMonthsBetween(y, m, todayYm); }
  else { lo = annMonthsBetween(y, 12, todayYm); hi = annMonthsBetween(y, 1, todayYm); }
  var okLo = annAgeFits(lo, age), okHi = annAgeFits(hi, age);
  if (okLo && okHi) return "pass";
  if (!okLo && !okHi) return "fail";
  return "unknown";                             // 범위가 기준선을 걸친다 → 확정 불가
}

// 상시근로자 — 실측 142/404 만 채워져 있다. 없으면 unknown 이 정상이다.
function annEmployeeState(co, emp) {
  if (!emp || (emp.min == null && emp.max == null)) return "pass";
  var n = parseInt(co && co.employee_count, 10);
  if (isNaN(n)) return "unknown";
  if (emp.min != null && n < emp.min) return "fail";
  if (emp.max != null && n > emp.max) return "fail";
  return "pass";
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **PASS** — `✅ 73/73 통과`

- [ ] **Step 5: 커밋**

```bash
git add src/App.js scripts/test-ann-match.mjs
git commit -F - <<'EOF'
feat(공고매칭): 📐 규모·매출·업력·근로자수 조건 판정

규모는 기존 judgeSmallBiz / judgeSososang 을 그대로 호출한다
(기업목록 규모 배지와 같은 함수 — 두 벌이 되면 반드시 어긋난다).

업력은 설립월이 없으면 최대 11개월 오차가 나므로 [최소,최대] 범위로 재고,
범위가 기준선을 걸칠 때만 unknown 으로 둔다.

node scripts/test-ann-match.mjs 73/73

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP
EOF
```

---

## Task 4: 종합 판정 · 전체 매칭 실행

**Files:**
- Modify: `src/App.js` — `annEmployeeState` 아래(ANN-ENGINE 블록 안)
- Modify: `scripts/test-ann-match.mjs` — 실제 DB 덤프 대조를 추가한다

**Interfaces:**
- Consumes: Task 1~3의 모든 `ann*State` 함수
- Produces:
  - `ANN_ENGINE_VERSION` = `1`
  - `annEvalCompany(co, cond, todayYm)` → `{ verdict: "유력"|"애매"|"제외", checks: [{key,label,state,got,want}], notes: string[] }`
  - `annRunMatch(companies, cond, ranBy)` → `{ summary: {ranAt,ranBy,engine,counts:{유력,애매,제외}}, rows: [...] }`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`EXPORTS` 에 더한다:

```js
const EXPORTS = [
  "normRegion", "annTrimGu", "annDeeperThanCity", "annRegionState",
  "annIndustryTokens", "annIndustryState",
  "annScaleState", "annRevenueState", "annAgeState", "annEmployeeState",
  "annEvalCompany", "annRunMatch", "ANN_ENGINE_VERSION",
];
```

마지막 `console.log` 줄 **바로 위**에 붙인다:

```js
// ── annEvalCompany / annRunMatch ────────────────────────────────────────────
const COND = {
  version: 1,
  regions:    { include: [{ sido: "서울특별시", sigungu: "강남구" }] },
  industries: { include: ["제조"], exclude: ["유흥"] },
  scale:      "소기업",
  revenue:    { min: null, max: 12000000000, basis: "연매출" },
  age:        { min_months: 12, max_months: null },
  employees:  null,
  excludes_text: ["국세·지방세 체납"],
};
const 유력업체 = { id: "c1", name: "가제조", region: "서울_강남", industry: "제조업",
                   revenue_2024: 3000000000, founded_year: 2019, founded_month: 4, assignee: "관호" };
const 애매업체 = { id: "c2", name: "나상사", region: "서울_강남", industry: "도소매업",
                   revenue_2024: 3000000000, founded_year: 2019, founded_month: 4, assignee: "관호" };
const 제외업체 = { id: "c3", name: "다식당", region: "경기_시흥", industry: "음식점업",
                   revenue_2024: 300000000, founded_year: 2019, founded_month: 4, assignee: "관호" };

const e1 = mod.annEvalCompany(유력업체, COND, 202608);
eq("유력 판정",       e1.verdict, "유력");
eq("유력은 검사 6개", e1.checks.length, 6);
const e2 = mod.annEvalCompany(애매업체, COND, 202608);
eq("애매 판정",       e2.verdict, "애매");
eq("애매 사유는 업종", e2.checks.filter(c => c.state === "unknown").map(c => c.key), ["industry"]);
const e3 = mod.annEvalCompany(제외업체, COND, 202608);
eq("제외 판정",       e3.verdict, "제외");
// ⚠️ 코드로 못 재는 제외조건은 판정을 바꾸지 않고 notes 로만 남긴다
eq("제외조건은 메모로", e1.notes.some(n => n.indexOf("국세") >= 0), true);
eq("업종 제외어 미확인 메모", e1.notes.some(n => n.indexOf("유흥") >= 0), true);

const run = mod.annRunMatch([유력업체, 애매업체, 제외업체, { id: "x", name: "지운곳", deleted_at: "2026-01-01" }], COND, "관호");
eq("삭제 기업 제외",  run.rows.length, 3);
eq("집계",            run.summary.counts, { 유력: 1, 애매: 1, 제외: 1 });
eq("엔진 버전",       run.summary.engine, mod.ANN_ENGINE_VERSION);
eq("정렬: 유력 먼저", run.rows.map(r => r.verdict), ["유력", "애매", "제외"]);
// ⚠️ 제외 행은 떨어진 조건 1개만 담는다 — 404개 × 6조건을 jsonb 에 넣지 않는다
eq("제외 행은 1개만", run.rows[2].checks.length, 1);
eq("제외 행은 fail만", run.rows[2].checks[0].state, "fail");

// ── 실제 DB 로 회귀 확인 (읽기 전용) ────────────────────────────────────────
// "전 조건이 비어 있는 공고"는 살아있는 기업 전부가 유력이어야 한다.
// 엔진이 조용히 업체를 흘리는지 보는 안전망이다.
const dump = loadCompanies();
const all = mod.annRunMatch(dump, { version: 1 }, "test");
eq("빈 조건이면 전원 유력", all.summary.counts.애매 + all.summary.counts.제외, 0);
eq("빈 조건이면 전원 포함", all.rows.length, dump.length);
console.log(`   (실제 DB 살아있는 기업 ${dump.length}개로 확인)`);
```

그리고 파일 상단 `import` 아래에 DB 덤프 함수를 추가한다(`test-nocard.mjs` 와 같은 방식):

```js
import os from "os";
import { execFileSync } from "child_process";

// 살아있는 기업을 읽어 온다(읽기 전용 SELECT 1개). .env.local 의 SUPABASE_ACCESS_TOKEN 필요.
function loadCompanies() {
  const sql = `select coalesce(json_agg(json_build_object(
      'id',id,'name',name,'region',region,'industry',industry,'assignee',assignee,
      'employee_count',employee_count,'founded_year',founded_year,'founded_month',founded_month,
      'revenue_2023',revenue_2023,'revenue_2024',revenue_2024,'revenue_2025',revenue_2025,
      'biz_match_override',biz_match_override,'deleted_at',deleted_at)),'[]'::json) as payload
    from public.companies where deleted_at is null;`;
  const tmp = path.join(os.tmpdir(), "ann-match-" + process.pid + ".sql");
  fs.writeFileSync(tmp, sql);
  let raw;
  try {
    raw = execFileSync("node", [path.join(ROOT, "scripts/run-sql.js"), tmp],
      { encoding: "utf8", maxBuffer: 64 * 1024 * 1024 });
  } catch (e) {
    console.error("❌ DB 조회 실패 — .env.local 의 SUPABASE_ACCESS_TOKEN 을 확인하세요.");
    console.error(String((e && e.stdout) || (e && e.message) || e).slice(-400));
    process.exit(1);
  } finally { try { fs.unlinkSync(tmp); } catch (_) {} }
  if (raw.indexOf("[") < 0) { console.error("❌ 조회 결과를 못 읽었습니다:\n" + raw.slice(-400)); process.exit(1); }
  return JSON.parse(raw.slice(raw.indexOf("[")))[0].payload;
}
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **FAIL** — `annEvalCompany is not defined`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/App.js` 의 `annEmployeeState` 닫는 `}` 아래(ANN-ENGINE 블록 안, `// ── ANN-ENGINE-END ──` 바로 위)에 넣는다:

```js
// 엔진이 바뀌면 옛 결과와 새 결과를 구분할 수 있어야 한다. 판정 규칙을 고치면 이 숫자를 올릴 것.
const ANN_ENGINE_VERSION = 1;

function annWon(v) {
  var n = Number(v);
  return n > 0 ? (n / 1e8).toFixed(1) + "억" : "";
}
function annWantRegion(c) {
  var list = ((c.regions || {}).include) || [];
  if (!list.length) return "전국";
  return list.map(function (r) { return ((r && r.sido) || "") + " " + ((r && r.sigungu) || ""); })
             .join(" · ").trim();
}

// 업체 하나 판정.
// 종합 규칙: fail 하나라도 → 제외 / fail 없고 unknown 없음 → 유력 / 나머지 → 애매.
// ⚠️ "확실" 등급은 만들지 않는다 — 서류로 확인해야 의미가 있고 그건 2차다.
function annEvalCompany(co, cond, todayYm) {
  var c = cond || {};
  var rev = c.revenue || null, age = c.age || null, emp = c.employees || null;
  var checks = [
    { key: "region", label: "지역", state: annRegionState(co && co.region, (c.regions || {}).include),
      got: (co && co.region) || "", want: annWantRegion(c) },
    { key: "industry", label: "업종", state: annIndustryState(co && co.industry, c.industries),
      got: (co && co.industry) || "", want: (((c.industries || {}).include) || []).join(" · ") || "제한 없음" },
    { key: "scale", label: "규모", state: annScaleState(co, c.scale),
      got: "", want: c.scale || "제한 없음" },
    { key: "revenue", label: "매출", state: annRevenueState(co, rev),
      got: annWon((co && co.revenue_2025) || (co && co.revenue_2024) || (co && co.revenue_2023)),
      want: rev ? ((rev.min != null ? annWon(rev.min) + " 이상 " : "") + (rev.max != null ? annWon(rev.max) + " 이하" : "")).trim() : "제한 없음" },
    { key: "age", label: "업력", state: annAgeState(co, age, todayYm),
      got: (co && co.founded_year) ? (co.founded_year + "년" + (co.founded_month ? " " + co.founded_month + "월" : "")) : "",
      want: age ? ((age.min_months != null ? age.min_months + "개월 이상 " : "") + (age.max_months != null ? age.max_months + "개월 이하" : "")).trim() : "제한 없음" },
    { key: "employees", label: "상시근로자", state: annEmployeeState(co, emp),
      got: (co && co.employee_count != null) ? String(co.employee_count) + "명" : "",
      want: emp ? ((emp.min != null ? emp.min + "명 이상 " : "") + (emp.max != null ? emp.max + "명 이하" : "")).trim() : "제한 없음" },
  ];

  var hasFail = false, hasUnknown = false;
  for (var i = 0; i < checks.length; i++) {
    if (checks[i].state === "fail") hasFail = true;
    else if (checks[i].state === "unknown") hasUnknown = true;
  }
  var verdict = hasFail ? "제외" : (hasUnknown ? "애매" : "유력");

  // ⚠️ 아래 메모는 **판정을 바꾸지 않는다.** 코드로 잴 수 없는 조건을 사람에게 넘기는 것이다.
  //    판정에 반영하면(전부 애매로 내리면) 기능이 죽고, 통과로 세면 거짓말이 된다.
  var notes = [];
  var exc = ((c.industries || {}).exclude) || [];
  var indState = checks[1].state;
  if (exc.length && indState !== "fail") {
    notes.push("⚠ 제외조건 미확인: " + exc.join(" · "));
  }
  ((c.excludes_text) || []).forEach(function (t) { if (t) notes.push("⚠ 확인 필요: " + t); });
  if (c.scale === "중소기업") {
    notes.push("ℹ 중소기업 여부는 확인하지 않았습니다(우리 고객은 전원 중소기업 전제)");
  }
  return { verdict: verdict, checks: checks, notes: notes };
}

// 전체 매칭. **supabase 를 부르지 않는다** — companies 는 이미 메모리에 있다.
// 그래서 재매칭이 즉시이고 비용이 0이다.
function annRunMatch(companies, cond, ranBy) {
  var kst = new Date(Date.now() + 9 * 3600 * 1000);
  var todayYm = kst.getUTCFullYear() * 100 + (kst.getUTCMonth() + 1);
  var counts = { 유력: 0, 애매: 0, 제외: 0 };
  var rows = [];
  (companies || []).forEach(function (co) {
    if (!co || co.deleted_at) return;
    var r = annEvalCompany(co, cond, todayYm);
    counts[r.verdict] += 1;
    // ⚠️ 제외 행은 떨어진 조건 1개만 담는다.
    //    전부 담으면 404개 × 6조건이 match_rows jsonb 에 들어간다(설계 §7-5).
    var isOut = r.verdict === "제외";
    rows.push({
      company_id: co.id, name: co.name || "", assignee: co.assignee || "",
      verdict: r.verdict,
      checks: isOut ? r.checks.filter(function (x) { return x.state === "fail"; }).slice(0, 1) : r.checks,
      notes: isOut ? [] : r.notes,
    });
  });
  var order = { "유력": 0, "애매": 1, "제외": 2 };
  rows.sort(function (a, b) {
    if (order[a.verdict] !== order[b.verdict]) return order[a.verdict] - order[b.verdict];
    var au = a.checks.filter(function (x) { return x.state === "unknown"; }).length;
    var bu = b.checks.filter(function (x) { return x.state === "unknown"; }).length;
    if (au !== bu) return au - bu;                       // 모르는 게 적은 업체를 위로
    return (a.name || "").localeCompare(b.name || "", "ko");
  });
  return {
    summary: { ranAt: new Date().toISOString(), ranBy: ranBy || "",
               engine: ANN_ENGINE_VERSION, counts: counts },
    rows: rows,
  };
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **PASS** — `✅ 88/88 통과` + `(실제 DB 살아있는 기업 404개로 확인)`

- [ ] **Step 5: 빌드 · 컬럼 오타 점검**

Run: `CI=true npx react-scripts build`
Expected: 빌드 성공(에러 0)

Run: `node scripts/audit-select-columns.mjs`
Expected: **0건** (이 태스크는 supabase select 를 늘리지 않는다)

- [ ] **Step 6: 커밋**

```bash
git add src/App.js scripts/test-ann-match.mjs
git commit -F - <<'EOF'
feat(공고매칭): ⚖️ 종합 판정(유력/애매/제외) + 전체 매칭 실행

fail 하나라도 → 제외 / fail 없고 unknown 없음 → 유력 / 나머지 → 애매.
"확실" 등급은 만들지 않는다 — 서류로 확인해야 의미가 있고 그건 2차다.

- supabase 호출 0건. companies 는 이미 메모리에 있어 재매칭이 즉시·무료다
- 제외 행은 떨어진 조건 1개만 담는다(404개 × 6조건을 jsonb 에 넣지 않는다)
- 코드로 못 재는 제외조건은 판정을 바꾸지 않고 notes 로만 남긴다

node scripts/test-ann-match.mjs 88/88 (실제 DB 404개 대조 포함)
CI=true npx react-scripts build 통과 · audit-select-columns 0건

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP
EOF
```

---

## Task 5: `announcements` 테이블 + RLS

**Files:**
- Create: `공고매칭_announcements_테이블추가.sql`
- Create: `공고매칭_announcements_테이블추가_rollback.sql`
- Create: `공고매칭_announcements_테이블추가_검증.sql`

**Interfaces:**
- Consumes: 없음 (엔진과 독립 — 엔진은 DB를 안 본다)
- Produces: `public.announcements` 테이블. 컬럼명은 설계 §5 그대로. 1-B 화면과 Task 6 API가 이 이름에 의존한다.

- [ ] **Step 1: 검증 SQL 을 먼저 쓴다 (실패해야 정상)**

Create `공고매칭_announcements_테이블추가_검증.sql`:

```sql
-- 📢 announcements 검증 — 조치 파일과 **따로** 실행할 것
--    (run-sql.js 는 마지막 SELECT 하나만 출력한다. CLAUDE.md 2-2)
--    실행: node scripts/run-sql.js 공고매칭_announcements_테이블추가_검증.sql
--    기대: 아래 7칸이 전부 true / anon_grants = 0 / extra_grants = 0
select
  (select count(*) from information_schema.tables
    where table_schema='public' and table_name='announcements') = 1            as 테이블존재,
  (select relrowsecurity from pg_class
    where relnamespace='public'::regnamespace and relname='announcements')     as rls켜짐,
  (select count(*) from pg_policies
    where schemaname='public' and tablename='announcements') = 4               as 정책4개,
  (select count(*) from pg_policies
    where schemaname='public' and tablename='announcements'
      and (roles::text like '%anon%' or roles::text like '%public%')) = 0       as anon정책없음,
  (select count(*) from information_schema.role_table_grants
    where table_schema='public' and table_name='announcements'
      and grantee='anon')                                                       as anon_grants,
  (select count(*) from information_schema.role_table_grants
    where table_schema='public' and table_name='announcements'
      and grantee='authenticated'
      and privilege_type in ('TRUNCATE','REFERENCES','TRIGGER'))                as extra_grants,
  (select count(*) from pg_indexes
    where schemaname='public' and tablename='announcements'
      and indexname='announcements_source_external_uniq') = 1                   as unique인덱스;
```

- [ ] **Step 2: 검증을 돌려 실패를 확인한다**

Run: `node scripts/run-sql.js 공고매칭_announcements_테이블추가_검증.sql`
Expected: `테이블존재: false` · `rls켜짐: null` · `정책4개: false` (테이블이 아직 없다)

- [ ] **Step 3: 테이블 SQL 을 쓴다**

Create `공고매칭_announcements_테이블추가.sql`:

```sql
-- ============================================================================
-- 📢 공고 매칭 — announcements 테이블 · RLS · 권한
--   설계: docs/superpowers/specs/2026-08-30-공고매칭-design.md §5
--   실행:     node scripts/run-sql.js 공고매칭_announcements_테이블추가.sql
--   검증:     node scripts/run-sql.js 공고매칭_announcements_테이블추가_검증.sql  ← 반드시 따로
--   되돌리기: 공고매칭_announcements_테이블추가_rollback.sql
--   프로젝트: ujdrjvnihxjvbkezjvwc
--
-- [왜 테이블이 하나뿐인가]
--   기관 디렉토리(support_agencies)는 이번 범위에서 빠졌다(2026-08-30 결정).
--   가리킬 테이블이 없는 source_agency_id 는 죽은 컬럼이 되므로 넣지 않는다.
--   나중에 그 기능을 할 때 `add column if not exists source_agency_id uuid` 한 줄로 붙는다.
--
-- [⚠️ match_summary / match_rows 를 나누는 이유]
--   목록 화면은 match_summary 만 select 한다. 자동 수집이 켜져 공고가 수백 건이 되면
--   둘을 같이 부르는 순간 목록 한 번에 수 MB 가 오간다.
--
-- [⚠️ unique 인덱스에 `where deleted_at is null` 을 넣지 않는 이유]
--   파이프라인 카드 휴지통과 같은 규칙이다. 사람이 "관심 없음"으로 지운 공고가
--   다음 자동 수집 때 되살아나면 안 된다. 수집기는 삭제분까지 보고 건너뛴다.
--
-- ⚠️ team_notes.is_announcement(팀 공지)와 아무 관계가 없다. 이름이 비슷할 뿐이다.
-- ============================================================================

begin;

create table if not exists public.announcements (
  id uuid primary key default gen_random_uuid(),

  -- 출처 (자동수집 확장의 이음매)
  source       text not null default 'manual',
  external_id  text,
  external_url text,

  -- 내용
  title           text not null,
  agency          text,
  support_summary text,
  apply_start     date,
  apply_end       date,

  -- 조건
  conditions       jsonb,   -- 확정본. 매칭이 보는 유일한 값
  conditions_ai    jsonb,   -- AI 추출 원본(읽기 전용 보존)
  conditions_state text not null default 'pending'
    check (conditions_state in ('pending', 'extracted', 'confirmed')),

  -- 매칭 결과
  match_summary jsonb,      -- 작다. 목록 배지용
  match_rows    jsonb,      -- 크다. 상세에서만 읽는다

  -- 관리
  status     text not null default 'active' check (status in ('active', 'closed')),
  created_by text,
  created_at timestamptz not null default now(),
  updated_at timestamptz,
  deleted_at timestamptz,

  constraint chk_announcements_title check (coalesce(btrim(title), '') <> ''),
  constraint chk_announcements_period
    check (apply_start is null or apply_end is null or apply_start <= apply_end)
);

comment on table public.announcements is
  '지원사업 공고. conditions 가 매칭의 유일한 입력이고 conditions_ai 는 AI 추출 원본 보존용.';
comment on column public.announcements.match_rows is
  '업체별 판정 상세. 목록 조회에서는 절대 select 하지 말 것 — 공고가 늘면 목록이 수 MB 가 된다.';

-- 같은 공고를 두 번 받지 않는다. 삭제분도 슬롯을 쥔다(위 주석 참고).
create unique index if not exists announcements_source_external_uniq
  on public.announcements (source, external_id);

create index if not exists idx_announcements_live
  on public.announcements (deleted_at, apply_end desc);   -- 목록·마감 배너

-- ── RLS — 새 테이블은 Supabase 기본값이 "열림"이다. 같은 커밋에서 켠다(CLAUDE.md 2-2).
--    새로 만드는 테이블이라 옛 정책은 없지만 관례대로 drop 후 create 한다.
alter table public.announcements enable row level security;

drop policy if exists p_announcements_select on public.announcements;
create policy p_announcements_select on public.announcements
  for select to authenticated using ((select public.is_approved()));

drop policy if exists p_announcements_insert on public.announcements;
create policy p_announcements_insert on public.announcements
  for insert to authenticated with check ((select public.is_approved()));

drop policy if exists p_announcements_update on public.announcements;
create policy p_announcements_update on public.announcements
  for update to authenticated
  using ((select public.is_approved())) with check ((select public.is_approved()));

-- DELETE 는 화면에서 soft delete(deleted_at)만 쓰지만, 휴지통 영구삭제를 1-B 에서
-- 붙일 수 있게 정책은 만들어 둔다(기업목록 휴지통과 같은 권한 = 승인된 팀원 전원).
drop policy if exists p_announcements_delete on public.announcements;
create policy p_announcements_delete on public.announcements
  for delete to authenticated using ((select public.is_approved()));

-- ── 권한 — anon 은 전부 회수, authenticated 도 필요한 것만 남긴다(CLAUDE.md 2-2)
revoke all on public.announcements from anon;
revoke all on public.announcements from authenticated;
grant select, insert, update, delete on public.announcements to authenticated;
-- TRUNCATE 는 RLS 도 트리거도 우회한다. REFERENCES/TRIGGER 는 앱이 쓰지 않는다.
revoke truncate, references, trigger on public.announcements from authenticated, anon;

commit;
```

- [ ] **Step 4: 되돌리기 SQL 을 쓴다**

Create `공고매칭_announcements_테이블추가_rollback.sql`:

```sql
-- 📢 announcements 되돌리기
--   ⚠️ 테이블을 통째로 지운다. 저장된 공고가 있으면 같이 사라진다.
--      실행 전에 남은 행이 있는지 반드시 확인할 것:
--        select count(*) from public.announcements where deleted_at is null;
--   실행: node scripts/run-sql.js 공고매칭_announcements_테이블추가_rollback.sql
begin;
drop policy if exists p_announcements_select on public.announcements;
drop policy if exists p_announcements_insert on public.announcements;
drop policy if exists p_announcements_update on public.announcements;
drop policy if exists p_announcements_delete on public.announcements;
drop table if exists public.announcements;
commit;
```

- [ ] **Step 5: 테이블을 만든다**

Run: `node scripts/run-sql.js 공고매칭_announcements_테이블추가.sql`
Expected: `✅ 실행 성공`

- [ ] **Step 6: 검증을 별도로 다시 돌린다**

Run: `node scripts/run-sql.js 공고매칭_announcements_테이블추가_검증.sql`
Expected: `테이블존재: true` · `rls켜짐: true` · `정책4개: true` · `anon정책없음: true` · `anon_grants: 0` · `extra_grants: 0` · `unique인덱스: true`

⚠️ 하나라도 어긋나면 멈추고 원인을 찾는다. 특히 `anon_grants` 가 0이 아니면 `revoke` 가
조용히 실패한 것이다(grantor 불일치 — CLAUDE.md 2-2).

- [ ] **Step 7: 커밋**

```bash
git add 공고매칭_announcements_테이블추가.sql 공고매칭_announcements_테이블추가_rollback.sql 공고매칭_announcements_테이블추가_검증.sql
git commit -F - <<'EOF'
feat(공고매칭): 🗄 announcements 테이블 + RLS + 권한 회수

설계 §5 그대로. 새 테이블은 만든 커밋에서 RLS 를 켠다(CLAUDE.md 2-2).

- match_summary / match_rows 를 나눠 목록 조회가 커지지 않게 했다
- unique(source, external_id)는 삭제분을 포함한다 — 사람이 지운 공고가
  자동수집으로 되살아나지 않게(파이프라인 카드 휴지통과 같은 규칙)
- 기관 디렉토리가 이번 범위에서 빠져 source_agency_id 는 넣지 않았다

검증 별도 실행 7/7 통과 (anon_grants 0 · extra_grants 0)

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP
EOF
```

---

## Task 6: 조건 추출 API + 응답 정규화

**Files:**
- Create: `api/parse-announcement.js`
- Create: `scripts/test-parse-announcement.mjs`
- Modify: `src/App.js` — ANN-ENGINE 블록 안, `annRunMatch` 아래
- Modify: `scripts/test-ann-match.mjs`

**Interfaces:**
- Consumes: `annRunMatch`(Task 4) — 정규화 결과가 그대로 들어간다
- Produces:
  - `POST /api/parse-announcement` — 요청 `{ fileBase64?, mediaType?, text? }` / 응답 `{ result: {...} }`
  - `annConditionsFromApi(raw)` → 매칭 엔진이 읽는 `conditions` 객체 (헬퍼 `annNum(v)` · `annStrList(v)` 포함)

### ⚠️ API 스키마는 **전부 문자열**이다 — 빈 문자열이 "공고에 없음"이다

`parse-credit.js` 가 이미 쓰는 방식이다("말하지 않은 값은 빈 문자열로 채우게 해 파싱 실패 여지를 없앤다").
스키마에 `type: ["number", "null"]` 같은 nullable 유니온을 넣지 않는 이유는
**그게 거절되면 정적 테스트로 못 잡고 배포 후에야 드러나기** 때문이다. 검증된 모양을 따른다.

숫자·null 로 바꾸는 곳은 `annConditionsFromApi` **한 곳뿐**이다.
⚠️ **`""` 를 `0` 으로 만들지 말 것** — "매출 0원 이하"라는 없던 조건이 생겨 전원이 제외된다.

⚠️ **`parse-credit.js` · `parse-financial.js` 를 고치지 않는다.** 형제 파일을 새로 만든다.
인증 블록이 5개 엔드포인트에 복제돼 있는 것은 의도된 구조다 — 공유 모듈(`api/_*.js`)은
Vercel 이 번들에 넣지 않아 함수가 아예 못 뜬다(2026-07-28 배포 사고).

- [ ] **Step 1: 실패하는 테스트를 쓴다 (API 정적 검증)**

Create `scripts/test-parse-announcement.mjs`:

```js
// api/parse-announcement.js 정적 검증 (2026-08-30)
// ⚠️ 실제 호출은 못 한다 — 서버 ANTHROPIC_API_KEY 가 필요하다(parse-credit 과 같은 제약).
//    배포 후 화면에서 실제 공고로 확인할 것.
// 여기서는 "빠지면 사고가 나는 것"만 본다: 인증 검사 · 스키마 키 이름 · nullable 미사용.
// 사용법: node scripts/test-parse-announcement.mjs
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const P = path.join(ROOT, "api/parse-announcement.js");
if (!fs.existsSync(P)) { console.error("❌ api/parse-announcement.js 가 없습니다"); process.exit(1); }
const src = fs.readFileSync(P, "utf8");

let pass = 0, fail = 0;
function ok(label, cond) {
  if (cond) { pass += 1; } else { fail += 1; console.error("❌ " + label); }
}

// ① 인증 — 없으면 누구나 호출해 ANTHROPIC_API_KEY 요금을 태울 수 있다(CLAUDE.md 2-2)
ok("denyUnauthorized 정의됨", /async function denyUnauthorized\(/.test(src));
ok("handler 안에서 호출됨", /if \(await denyUnauthorized\(req, res\)\) return;/.test(src));
ok("POST 만 허용", /req\.method !== "POST"/.test(src));

// ② 공유 모듈 금지 — Vercel 이 api/_*.js 를 번들에 안 넣어 함수가 못 뜬다
ok("_ 시작 모듈 import 없음", !/from\s+["']\.\/_/.test(src));

// ③ 안전장치 — 반쪽 결과를 정상처럼 넘기지 않는다
ok("refusal 처리", /stop_reason === "refusal"/.test(src));
ok("max_tokens 처리", /stop_reason === "max_tokens"/.test(src));

// ④ 스키마 — 표식 사이를 떼어내 **그대로 실행**한다.
//    ⚠️ JSON.parse 로는 못 읽는다(REGION 같은 상수를 참조하는 JS 객체 리터럴이다).
const a = src.indexOf("// ── ANN-SCHEMA-START ──");
const b = src.indexOf("// ── ANN-SCHEMA-END ──");
ok("SCHEMA 표식 있음", a >= 0 && b > a);
let schema = null;
if (a >= 0 && b > a) {
  try { schema = new Function(src.slice(a, b) + "\nreturn SCHEMA;")(); }
  catch (e) { console.error("   (스키마 실행 실패: " + e.message + ")"); }
}
ok("SCHEMA 를 읽음", !!schema);
if (schema) {
  const top = Object.keys(schema.properties || {});
  ["title", "agency", "support_summary", "apply_start", "apply_end", "conditions"]
    .forEach(k => ok("최상위 키 " + k, top.indexOf(k) >= 0));
  const cond = Object.keys(((schema.properties || {}).conditions || {}).properties || {});
  // ⚠️ 이 이름들은 annConditionsFromApi 가 그대로 읽는다. 하나라도 다르면 조건이 조용히 무시된다.
  ["regions", "industries", "scale", "revenue", "age", "employees", "excludes_text", "quotes"]
    .forEach(k => ok("conditions 키 " + k, cond.indexOf(k) >= 0));
  ok("quotes 가 required", (((schema.properties || {}).conditions || {}).required || []).indexOf("quotes") >= 0);
}
// ⚠️ nullable 유니온을 쓰면 구조화 출력이 거절될 수 있고, 그건 배포 후에야 드러난다.
ok("nullable 타입 미사용", a >= 0 && b > a && !/type:\s*\[/.test(src.slice(a, b)));

// ⑤ 모델 — 문서 추출은 parse-credit·parse-financial 과 같은 등급을 쓴다
ok("claude-opus-5", /const MODEL = "claude-opus-5";/.test(src));

console.log("\n" + (fail === 0 ? "✅" : "❌") + " " + pass + "/" + (pass + fail) + " 통과");
process.exit(fail === 0 ? 0 : 1);
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `node scripts/test-parse-announcement.mjs`
Expected: **FAIL** — `❌ api/parse-announcement.js 가 없습니다`

- [ ] **Step 3: API 를 쓴다**

Create `api/parse-announcement.js`:

```js
// Vercel 서버리스 함수: 지원사업 공고(PDF/이미지/텍스트) → 자격조건 구조화 추출
// 위치: 프로젝트 루트의 api/parse-announcement.js
// 환경변수 ANTHROPIC_API_KEY 필요 (Vercel에 등록됨)
//
// 형제 엔드포인트(parse-credit/parse-financial/ai-search/…)와 동일하게 raw fetch 를 쓰고,
// 파일 하나로 완결한다. api/_*.js 공유 모듈은 Vercel 이 번들에 넣지 않아 함수가 못 뜬다.
//
// 설계 원칙 (docs/superpowers/specs/2026-08-30-공고매칭-design.md §6):
//   · 이 함수는 "추출"만 한다. 판정은 프런트(App.js annEvalCompany)가 코드로 한다.
//   · **말하지 않은 것은 빈 문자열로 둔다.** "전국"이나 0 으로 채우면 없던 조건이 생겨
//     판정이 통째로 틀린다(옛 사고 계열: "1억" → 1원, 커밋 d3df072).
//   · quotes(조건별 공고 원문)는 필수다. 사람이 조건을 고칠 때도, 근거를 볼 때도 필요하다.
//
// ⚠️ 스키마는 전부 문자열이다. nullable 유니온(type:["number","null"])을 쓰지 않는 이유는
//    거절될 경우 정적 테스트로 못 잡고 배포 후에야 드러나기 때문이다.
//    숫자·null 변환은 App.js annConditionsFromApi 한 곳에서만 한다.
//
// ── 인증 검사 (2026-07-28 보안조치 8) ────────────────────────────────────────
// ⚠️ 형제 엔드포인트에 똑같은 블록이 들어 있다. 고칠 때는 함께 고칠 것.
const SUPABASE_URL = "https://ujdrjvnihxjvbkezjvwc.supabase.co";

async function denyUnauthorized(req, res) {
  const deny = (status, error) => { res.status(status).json({ error: error }); return true; };

  const auth = req.headers.authorization || "";
  const token = auth.startsWith("Bearer ") ? auth.slice(7) : "";
  if (!token) return deny(401, "로그인이 필요합니다.");
  const anonKey = req.headers["x-supabase-anon"] || "";
  if (!anonKey) return deny(401, "인증 정보가 없습니다.");

  let userRes;
  try {
    userRes = await fetch(SUPABASE_URL + "/auth/v1/user", {
      headers: { apikey: anonKey, Authorization: "Bearer " + token },
    });
  } catch (e) {
    return deny(503, "인증 서버에 연결할 수 없습니다.");
  }
  if (!userRes.ok) return deny(401, "로그인이 만료되었습니다. 새로고침 후 다시 시도해주세요.");
  const user = await userRes.json();
  if (!user || !user.id) return deny(401, "로그인이 필요합니다.");

  const profRes = await fetch(
    SUPABASE_URL + "/rest/v1/profiles?select=status&id=eq." + encodeURIComponent(user.id),
    { headers: { apikey: anonKey, Authorization: "Bearer " + token } }
  );
  if (!profRes.ok) return deny(403, "권한을 확인할 수 없습니다.");
  const rows = await profRes.json();
  if (!Array.isArray(rows) || !rows[0] || rows[0].status !== "approved") {
    return deny(403, "관리자 승인 후 이용할 수 있습니다.");
  }
  return false;
}

const MODEL = "claude-opus-5";

// ── ANN-SCHEMA-START ── (scripts/test-parse-announcement.mjs 가 이 사이를 떼어내 실행한다)
const STR = { type: "string" };
const REGION = {
  type: "object", additionalProperties: false,
  required: ["sido", "sigungu"],
  properties: {
    sido: { type: "string", description: "시·도 이름을 공고 원문 그대로(예: 서울특별시, 경기도)" },
    sigungu: { type: "string", description: "시·군·구. 공고가 더 좁게 한정하면 그대로(예: 고양시 덕양구). 시도까지만이면 빈 문자열" },
  },
};

const SCHEMA = {
  type: "object", additionalProperties: false,
  required: ["title", "agency", "support_summary", "apply_start", "apply_end", "conditions"],
  properties: {
    title: { type: "string", description: "공고 제목" },
    agency: { type: "string", description: "소관기관. 없으면 빈 문자열" },
    support_summary: { type: "string", description: "지원 내용을 두 문장 이내로" },
    apply_start: { type: "string", description: "신청 시작일 YYYY-MM-DD. 없으면 빈 문자열" },
    apply_end: { type: "string", description: "신청 마감일 YYYY-MM-DD. 없으면 빈 문자열" },
    conditions: {
      type: "object", additionalProperties: false,
      required: ["version", "regions", "industries", "scale", "revenue", "age", "employees", "excludes_text", "quotes"],
      properties: {
        version: { type: "string", description: "항상 1" },
        regions: {
          type: "object", additionalProperties: false, required: ["include", "note"],
          properties: {
            include: { type: "array", items: REGION, description: "대상 지역. 전국이거나 지역 제한이 안 적혀 있으면 빈 배열" },
            note: STR,
          },
        },
        industries: {
          type: "object", additionalProperties: false, required: ["include", "exclude", "ksic"],
          properties: {
            include: { type: "array", items: STR, description: "대상 업종 키워드. 동의어·하위업종을 넉넉히 넣는다" },
            exclude: { type: "array", items: STR, description: "제외 업종 키워드. 공고가 실제로 배제한 것만" },
            ksic: { type: "array", items: STR, description: "한국표준산업분류 대분류 알파벳. 모르면 빈 배열" },
          },
        },
        scale: { type: "string", enum: ["소상공인", "소기업", "중소기업", ""], description: "규모 제한. 없으면 빈 문자열" },
        revenue: {
          type: "object", additionalProperties: false, required: ["min", "max", "basis"],
          properties: {
            min: { type: "string", description: "매출 하한을 원 단위 숫자 문자열로. 없으면 빈 문자열" },
            max: { type: "string", description: "매출 상한을 원 단위 숫자 문자열로(120억이면 12000000000). 없으면 빈 문자열" },
            basis: { type: "string", enum: ["연매출", "3년평균", ""] },
          },
        },
        age: {
          type: "object", additionalProperties: false, required: ["min_months", "max_months", "basis"],
          properties: {
            min_months: { type: "string", description: "업력 하한을 개월 수 문자열로(1년 이상이면 12). 없으면 빈 문자열" },
            max_months: { type: "string", description: "업력 상한을 개월 수 문자열로. 없으면 빈 문자열" },
            basis: STR,
          },
        },
        employees: {
          type: "object", additionalProperties: false, required: ["min", "max"],
          properties: {
            min: { type: "string", description: "상시근로자 하한. 없으면 빈 문자열" },
            max: { type: "string", description: "상시근로자 상한. 없으면 빈 문자열" },
          },
        },
        excludes_text: { type: "array", items: STR, description: "코드로 잴 수 없는 제외 조건을 원문 그대로" },
        quotes: {
          type: "object", additionalProperties: false,
          required: ["regions", "industries", "scale", "revenue", "age", "employees", "excludes"],
          properties: {
            regions: STR, industries: STR, scale: STR, revenue: STR,
            age: STR, employees: STR, excludes: STR,
          },
        },
      },
    },
  },
};
// ── ANN-SCHEMA-END ──

const SYSTEM = [
  "당신은 정부·지자체 지원사업 공고문에서 신청 자격 조건만 정확히 뽑아내는 도구입니다.",
  "",
  "★★ 가장 중요한 규칙 — 말하지 않은 것은 채우지 않습니다 ★★",
  "  공고에 적혀 있지 않은 조건은 반드시 빈 문자열(또는 빈 배열)로 두세요.",
  "  '보통 그럴 것'이라고 채우면 없던 조건이 생겨 대상 기업이 통째로 잘못 걸러집니다.",
  "  지역 제한이 안 적혀 있으면 regions.include 는 빈 배열입니다('전국'이라고 지어내지 마세요).",
  "  숫자 칸도 마찬가지입니다. 조건이 없으면 0 이 아니라 빈 문자열입니다.",
  "",
  "금액은 원 단위 숫자 문자열로 바꿔 담습니다. 120억원이면 12000000000, 5천만원이면 50000000.",
  "업력은 개월 수 문자열로 바꿔 담습니다. 업력 1년 이상이면 age.min_months 는 12 입니다.",
  "",
  "industries.include 는 넉넉히 적으세요.",
  "  우리 쪽 업종 데이터는 표준분류 코드가 아니라 자유 텍스트입니다(금속가공업, 통신기기 소매업).",
  "  제조업 영위 기업이면 include 에 제조·가공·생산과 함께 대표적인 하위업종 이름도 넣어 주세요.",
  "  반대로 exclude 는 공고가 실제로 배제한다고 적은 것만 넣습니다.",
  "",
  "지역은 공고가 좁힌 만큼 그대로 적습니다.",
  "  고양시 덕양구 소재 기업이면 sigungu 에 '고양시 덕양구'라고 그대로 쓰세요.",
  "  시·도까지만 한정하면 sigungu 는 빈 문자열입니다.",
  "",
  "excludes_text 에는 기계가 잴 수 없는 조건을 원문 그대로 담습니다",
  "  (국세·지방세 체납, 휴·폐업, 금융기관 연체, 중복 수혜 제한 등).",
  "",
  "quotes 에는 각 조건을 어디서 읽었는지 공고 원문을 그대로 인용합니다.",
  "  사람이 이 인용을 보고 조건을 고칩니다. 요약하지 말고 문장을 그대로 옮기세요.",
  "  해당 조건이 공고에 없으면 빈 문자열로 둡니다.",
].join("\n");

export default async function handler(req, res) {
  if (req.method !== "POST") {
    res.status(405).json({ error: "POST 요청만 허용됩니다." });
    return;
  }
  if (await denyUnauthorized(req, res)) return;

  try {
    const { fileBase64, mediaType, text } = req.body || {};
    const plain = String(text || "").trim();
    const mt = String(mediaType || "");
    const isPdf = mt === "application/pdf";
    const isImage = /^image\/(png|jpeg|jpg|gif|webp)$/.test(mt);

    if (!fileBase64 && !plain) {
      res.status(400).json({ error: "공고 파일이나 본문 텍스트 중 하나가 필요합니다." });
      return;
    }
    if (fileBase64 && !isPdf && !isImage) {
      res.status(400).json({ error: "PDF 또는 이미지(PNG/JPG) 파일만 첨부할 수 있어요." });
      return;
    }

    const apiKey = process.env.ANTHROPIC_API_KEY;
    if (!apiKey) {
      res.status(500).json({ error: "서버에 API 키가 설정되지 않았습니다." });
      return;
    }

    const askText = "이 지원사업 공고에서 신청 자격 조건을 스키마에 맞춰 뽑아 주세요."
      + " 공고에 없는 조건은 반드시 빈 문자열 또는 빈 배열로 두고, 각 조건의 근거 문장을 quotes 에 그대로 인용해 주세요.";

    const content = [];
    if (fileBase64) {
      content.push(isPdf
        ? { type: "document", source: { type: "base64", media_type: "application/pdf", data: fileBase64 } }
        : { type: "image", source: { type: "base64", media_type: mt === "image/jpg" ? "image/jpeg" : mt, data: fileBase64 } });
    }
    if (plain) content.push({ type: "text", text: "공고 본문:\n" + plain });
    content.push({ type: "text", text: askText });

    const r = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "content-type": "application/json",
        "x-api-key": apiKey,
        "anthropic-version": "2023-06-01",
      },
      body: JSON.stringify({
        model: MODEL,
        max_tokens: 16000,
        system: SYSTEM,
        output_config: { format: { type: "json_schema", schema: SCHEMA } },
        messages: [{ role: "user", content: content }],
      }),
    });

    const data = await r.json();
    if (!r.ok) {
      const msg = (data && data.error && data.error.message) || "공고 분석에 실패했습니다.";
      res.status(r.status).json({ error: msg });
      return;
    }
    // 안전장치: 거절되거나 잘렸으면 반쪽 결과를 정상처럼 넘기지 않는다
    if (data.stop_reason === "refusal") {
      res.status(422).json({ error: "이 문서는 분석할 수 없습니다. 다른 파일로 시도해주세요." });
      return;
    }
    if (data.stop_reason === "max_tokens") {
      res.status(422).json({ error: "공고가 너무 길어 전부 읽지 못했습니다. 자격요건 부분만 잘라 첨부해주세요." });
      return;
    }

    const textBlock = (data.content || []).find(function (b) { return b.type === "text"; });
    if (!textBlock || !textBlock.text) {
      res.status(502).json({ error: "공고에서 자격 조건을 찾지 못했습니다." });
      return;
    }
    let parsed;
    try {
      parsed = JSON.parse(textBlock.text);
    } catch (e) {
      res.status(502).json({ error: "분석 결과를 해석하지 못했습니다." });
      return;
    }

    res.status(200).json({ result: parsed });
  } catch (err) {
    res.status(500).json({ error: "서버 오류: " + (err && err.message ? err.message : String(err)) });
  }
}
```

- [ ] **Step 4: API 정적 테스트를 돌려 통과를 확인한다**

Run: `node scripts/test-parse-announcement.mjs`
Expected: **PASS** — `✅ 25/25 통과`

- [ ] **Step 5: 정규화 함수의 실패하는 테스트를 쓴다**

`scripts/test-ann-match.mjs` 의 `EXPORTS` 에 `annConditionsFromApi` 를 더한다:

```js
const EXPORTS = [
  "normRegion", "annTrimGu", "annDeeperThanCity", "annRegionState",
  "annIndustryTokens", "annIndustryState",
  "annScaleState", "annRevenueState", "annAgeState", "annEmployeeState",
  "annEvalCompany", "annRunMatch", "ANN_ENGINE_VERSION",
  "annConditionsFromApi",
];
```

마지막 `console.log` 줄 **바로 위**에 붙인다(Task 4가 만든 `유력업체`·`애매업체`·`제외업체` 를 그대로 쓴다):

```js
// ── annConditionsFromApi ────────────────────────────────────────────────────
// ⚠️ API 스키마는 전부 문자열이다. "" 는 "공고에 없음"이지 0 이 아니다.
const F = mod.annConditionsFromApi;
const empty = F({ conditions: {
  version: "1", regions: { include: [], note: "" },
  industries: { include: [], exclude: [], ksic: [] }, scale: "",
  revenue: { min: "", max: "", basis: "" }, age: { min_months: "", max_months: "", basis: "" },
  employees: { min: "", max: "" }, excludes_text: [], quotes: {} } });
eq("빈 값 → 지역 조건 없음",   empty.regions.include, []);
eq("빈 값 → 규모 조건 없음",   empty.scale,           null);
eq("빈 값 → 매출 조건 없음",   empty.revenue,         null);
eq("빈 값 → 업력 조건 없음",   empty.age,             null);
eq("빈 값 → 근로자 조건 없음", empty.employees,       null);
// ⚠️ ""를 0으로 만들면 "매출 0원 이하"라는 없던 조건이 생겨 전원이 제외된다
eq("빈 조건이면 전원 유력",
   mod.annRunMatch([유력업체, 애매업체, 제외업체], empty, "t").summary.counts,
   { 유력: 3, 애매: 0, 제외: 0 });

const filled = F({ conditions: {
  regions: { include: [{ sido: "서울특별시", sigungu: "강남구" }, { sido: "", sigungu: "" }] },
  industries: { include: ["제조", "  "], exclude: [], ksic: [] }, scale: "소기업",
  revenue: { min: "", max: "12,000,000,000", basis: "연매출" },
  age: { min_months: "12", max_months: "", basis: "설립일" },
  employees: { min: "", max: "" }, excludes_text: ["국세 체납", ""], quotes: { regions: "서울 소재" } } });
eq("시도 없는 지역은 버린다", filled.regions.include.length, 1);
eq("콤마 있는 금액",         filled.revenue.max,        12000000000);
eq("업력 하한만",            filled.age,                { min_months: 12, max_months: null, basis: "설립일" });
eq("빈 키워드 제거",         filled.industries.include, ["제조"]);
```

- [ ] **Step 6: 테스트를 돌려 실패를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **FAIL** — `annConditionsFromApi is not defined`

- [ ] **Step 7: 정규화 함수를 쓴다**

`src/App.js` 의 `annRunMatch` 닫는 `}` 아래(ANN-ENGINE 블록 안, `// ── ANN-ENGINE-END ──` 바로 위)에 넣는다:

```js
// /api/parse-announcement 응답 → 매칭 엔진이 읽는 conditions.
// ⚠️ API 스키마는 **전부 문자열**이다("" = 공고에 없음). nullable 유니온을 스키마에 넣으면
//    거절될 때 배포 후에야 드러나므로, parse-credit 과 같은 방식을 따랐다.
//    숫자·null 로 바꾸는 곳은 여기 한 곳뿐이다.
// ⚠️ **"" 를 0 으로 만들지 말 것** — "매출 0원 이하"라는 없던 조건이 생겨 전원이 제외된다.
function annNum(v) {
  var s = String(v == null ? "" : v).replace(/[,\s원]/g, "").trim();
  if (!s) return null;
  var n = Number(s);
  return isFinite(n) ? n : null;
}
function annStrList(v) {
  return Array.isArray(v)
    ? v.map(function (x) { return String(x == null ? "" : x).trim(); }).filter(Boolean)
    : [];
}
function annConditionsFromApi(raw) {
  var c = (raw && raw.conditions) || {};
  var reg = c.regions || {}, ind = c.industries || {};
  var rev = c.revenue || {}, age = c.age || {}, emp = c.employees || {};
  var revMin = annNum(rev.min), revMax = annNum(rev.max);
  var ageMin = annNum(age.min_months), ageMax = annNum(age.max_months);
  var empMin = annNum(emp.min), empMax = annNum(emp.max);
  return {
    version: 1,
    regions: {
      include: (Array.isArray(reg.include) ? reg.include : []).map(function (r) {
        return { sido: String((r && r.sido) || "").trim(), sigungu: String((r && r.sigungu) || "").trim() };
      }).filter(function (r) { return !!r.sido; }),   // 시도가 없으면 지역으로 못 쓴다
      note: String(reg.note || ""),
    },
    industries: {
      include: annStrList(ind.include), exclude: annStrList(ind.exclude), ksic: annStrList(ind.ksic),
    },
    scale: ["소상공인", "소기업", "중소기업"].indexOf(String(c.scale || "")) >= 0 ? String(c.scale) : null,
    revenue: (revMin == null && revMax == null) ? null
      : { min: revMin, max: revMax, basis: String(rev.basis || "연매출") },
    age: (ageMin == null && ageMax == null) ? null
      : { min_months: ageMin, max_months: ageMax, basis: String(age.basis || "설립일") },
    employees: (empMin == null && empMax == null) ? null : { min: empMin, max: empMax },
    excludes_text: annStrList(c.excludes_text),
    quotes: (c.quotes && typeof c.quotes === "object") ? c.quotes : {},
  };
}
```

- [ ] **Step 8: 테스트를 돌려 통과를 확인한다**

Run: `node scripts/test-ann-match.mjs`
Expected: **PASS** — `✅ 98/98 통과`

- [ ] **Step 9: 빌드**

Run: `CI=true npx react-scripts build`
Expected: 빌드 성공(에러 0)

- [ ] **Step 10: 커밋**

파일이 여러 개라 한 번에 담는다.

```
git add api/parse-announcement.js scripts/test-parse-announcement.mjs src/App.js scripts/test-ann-match.mjs
```

커밋 메시지(제목 + 본문):

```
feat(공고매칭): 🤖 공고 → 자격조건 추출 API + 응답 정규화

parse-credit.js 의 형제. 파일 하나로 완결하고 첫 줄에 denyUnauthorized 를 둔다.
parse-credit.js·parse-financial.js 는 한 줄도 고치지 않았다.

- 스키마는 전부 문자열이고 빈 문자열이 "공고에 없음"이다.
  nullable 유니온을 쓰지 않는 이유는 거절될 경우 정적 테스트로 못 잡고
  배포 후에야 드러나기 때문이다(parse-credit 과 같은 방식).
- 숫자·null 변환은 annConditionsFromApi 한 곳에서만 한다.
  빈 문자열을 0으로 만들면 "매출 0원 이하"라는 없던 조건이 생겨 전원이 제외된다.
- quotes(조건별 공고 원문)는 required. 사람이 이걸 보고 조건을 고친다
- industries.include 는 넉넉히 뽑게 한다 — 우리 업종은 KSIC 가 아니라 자유텍스트다
- refusal / max_tokens 를 막아 반쪽 결과를 정상처럼 넘기지 않는다

node scripts/test-parse-announcement.mjs 25/25
node scripts/test-ann-match.mjs 98/98 · build 통과

배포 후 실제 공고 PDF 로 화면에서 확인할 것 (서버 API 키가 필요해 로컬에서 못 돈다).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MYpce9upyT8TNGANMoQrDP
```

---

## 1-A 완료 조건 (회귀 점검 포함)

전 태스크가 끝나면 아래를 **전부** 확인하고 보고한다(CLAUDE.md 3절 — "회귀 확인 완료" 여부를 명시할 것).

- [ ] `node scripts/test-ann-match.mjs` — 98/98 (실제 DB 404개 대조 포함)
- [ ] `node scripts/test-parse-announcement.mjs` — 25/25
- [ ] `node scripts/run-sql.js 공고매칭_announcements_테이블추가_검증.sql` — 7칸 전부 정상
- [ ] `node scripts/audit-select-columns.mjs` — 0건
- [ ] `CI=true npx react-scripts build` — 에러 0
- [ ] **기업목록 규모/기관 배지가 그대로인가** — `bizScaleCap`·`judgeSmallBiz`·`judgeSososang` 을 고치지 않았음을 `git diff` 로 확인(BIZ-SCALE 표식 주석 2줄 외 변경 0줄)
- [ ] `git diff` 에 `api/parse-credit.js`·`api/parse-financial.js` 변경 0줄
- [ ] 기존 테스트 회귀: `node scripts/test-amount-unit.mjs` · `node scripts/test-debtor-change.mjs` · `node scripts/test-nocard.mjs` · `node scripts/test-revenue-input.mjs`

**1-A 는 화면이 없다.** 배포해도 사용자에게 보이는 변화가 0이다 — 그게 정상이다.
실제 공고로 확인하는 것은 1-B(화면)에서 한다.

## 다음 차수에서 다시 볼 것 (지금 정하지 않았다)

- **업종 `unknown` 이 너무 많이 나오면** — 첫 실제 공고를 돌려 보고, include 키워드를 넉넉히 뽑는 것만으로 부족하면 그때 KSIC 대분류 매핑표를 별도 태스크로 만든다. 지금 억지로 넣으면 "금속가공업이 제조업인 줄 모르는" 오탐이 난다.
- **`normRegion` 이 시군구 자리에 쓰레기를 넣는 경우**(`대전지역본부`) — 매칭이 안 되어 `unknown` 으로 빠지므로 안전한 방향이다. 229개 시군구 표는 필요해지면 그때 만든다.
