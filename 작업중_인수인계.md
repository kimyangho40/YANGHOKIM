# 작업중 인수인계 — 부결 체크리스트 / 스크립트·교육 체크박스 / 사례집 기관탭

작성: 2026-07-16 · 상태: **조사 완료, 코드 미착수** (src/App.js 수정 0줄)

세 작업 모두 `src/App.js` (15,077줄 단일 파일) 안에 있음.
빌드 베이스라인 확인됨 — `npm run build` 정상 (261.52 kB).

---

## 확정된 결정사항

- SQL은 `재신청체크리스트_스크립트교육_컬럼추가.sql` 로 작성 완료. **아직 실행 안 함** → Supabase SQL Editor에서 직접 실행 필요.
- 재신청 건 날짜 기준: **원본 건의 year/month +1개월** (12월 → 다음해 1월). 오늘 기준 아님.
- 사례집 기관 탭: 지정한 9개 + 데이터에 실제 존재하는 기존값(구조혁신&사업전환/경정청구/기타) 자동 append → 기존 사례가 필터에서 사라지지 않게.

---

## [1] 부결/반려 재신청 체크리스트

**컴포넌트**: `AgencyView` (11057줄~)

status를 부결/반려로 바꿀 수 있는 경로가 **2곳** — 둘 다 가로채야 함:

| 위치 | 줄 | 설명 |
|---|---|---|
| 사이드패널 상태 버튼 | 12367 | 주 경로. `["시작 전","진행 중","보류","부결","승인","완료"]` — **반려 없음** |
| 인라인 편집 select → `saveEdit` | 11427, 11984 | `STATUS_OPTIONS`(11482) 사용 — 부결·반려 **둘 다 있음** |

- 사이드패널 상태 변경 시 `activity_logs` 자동 기록 로직이 이미 있음(12374~12395) → 재신청 생성도 같은 자리에 붙이면 됨. 컬럼 없을 때 최소 필드로 재시도하는 방어 패턴 참고할 것.
- **모달 패턴 선례**: `priority_checks` 모달 (11531~11549, `showPriorityModal`/`priorityTarget`/`priorityChecks` + `openPriority`/`savePriorityChecks`). 이걸 그대로 본떠서 `showRejectModal`/`rejectTarget`/`rejectChecks` 만들면 됨.
  - 단 `priority_checks`는 **text 컬럼에 JSON.stringify**로 저장 중. `reject_checklist`는 **jsonb**라 stringify 하지 말 것 (객체 그대로 전달).
- 재신청 건 insert는 `saveNewCase`(11401) 의 insertData 형태 참고.
- 배지: `reapply_from_id != null` 이면 "재신청" 배지. 중복 배지(12001~12005)가 같은 셀 패턴.

## [2] script_delivered / phone_education_done

- 테이블 헤더: 11771~11799 (`headerCellStyle` + `ResizeHandle colKey` 패턴 — colWidths에 키 추가 필요)
- 행 렌더: 11802~ (`filtered.map`)
- 토글 즉시 저장 선례: 11961~11970 (contact_phones X버튼) — `supabase.update` 후 `setCases` 낙관적 갱신.
- 행 클릭 시 `setSelectedCase(row)` 열리므로 체크박스에 **`e.stopPropagation()` 필수**.

## [3] 사례집 기관 탭 — SQL 불필요

**컴포넌트**: `ApprovalCasesView` (13292줄~)

- `approval_cases.agency_group` 컬럼 **이미 존재**, 필터도 이미 동작 중 (13322).
- 현재 드롭다운: 13489~13491. `agencyOpts` 배열은 13350.
- 이걸 탭으로 교체. **탭 UI 선례**: 활동로그 기관 pill 탭 (7554~7556) — 그대로 복사 가능.
- 현재 `agencyOpts`에 없는 것: 미소재단, 수출무역보험공사 → 추가 필요.
- 참고: `AGENCY_GROUPS` 상수 391~401 (기관별 색상 정의) — 탭 색상에 재활용 가능.
- 부결 시 사례집 자동 초안 생성 로직이 2186~2222에 있음 (`agency_group: rest.agency`) — [1] 작업과 겹치는 영역이니 주의.

---

## 다음 할 일

1. Supabase에서 `재신청체크리스트_스크립트교육_컬럼추가.sql` 실행
2. [1] → build → [2] → build → [3] → build
3. 전부 끝나면 한번에 커밋 (push는 확인 후)
